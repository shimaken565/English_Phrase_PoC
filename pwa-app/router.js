const routes=new Map();let current,root;const routeName=path=>path.split('?')[0]||'home';export function initRouter(element){root=element;addEventListener('popstate',()=>render(location.hash.slice(1)||'home'));return render(location.hash.slice(1)||'home')}export function route(path){history.pushState({},'',`#${path}`);return render(path)}export function register(path,renderPage){routes.set(path,renderPage)}export function render(path){current=path;const page=routes.get(routeName(path))||routes.get('home');root.replaceChildren(page());return current}
